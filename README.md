# Platform Event Mock
Creates mock events in the database
## Config
See `.env` file
## Environment
Start database and import SQL dump
```BASH
$ docker-compose up -d
$ chmod +x import.sh && ./import.sh
```